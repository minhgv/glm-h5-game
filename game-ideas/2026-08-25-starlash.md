# STARLASH — Neon Gravity Lasso

- **Ngày:** 2026-08-25
- **Genre:** Arcade/Casual One-Thumb — Endless Ascent; core mechanics: Elastic Gravity Lasso (giữ để neo dây đàn hồi, thả để slingshot), Gravity Well orbit, Graze-Combo scoring, Perfect-Sling timing
- **Mô tả:** Một ngón tay của bạn là một lasso hấp dẫn: giữ để neo dây đàn hồi vào không gian, quăng sao chổi xoay quanh các giếng trọng lực, thả đúng nhịp để slingshot và cạo sát tiểu hành tinh nhằm nhân combo neon. Cảm giác chơi như «đánh cá bằng vật lý» giữa deep-space: dễ học trong 5 giây, khó master, mỗi cú Perfect Sling đã tay như nổ bắn bi trên trời.
- **Nguồn:** GLM-5.3 (Z.AI Coding Plan)

---

## Master Prompt (Production-Ready)

```text
MASTER PROMPT — STARLASH (H5 Arcade — One-Thumb Physics Lasso)

=== /goal ===
Ship STARLASH ở chuẩn Enterprise Arcade Game Studio: game H5 thương mại hoá được (điểm chèn ads giữa các run, rewarded-retry), core mechanic hoàn toàn mới — Elastic Gravity Lasso — chưa có trên thị trường H5, retention kiểu one-more-run (best score, grade S–D, near-miss replay), và chất lượng vận hành phân biệt tuyệt đối với game học sinh/amateur: juice chuẩn AAA-mobile, audio procedural 100%, performance budget kỷ luật. Sản phẩm cuối: duy nhất 1 file index.html, mở là chơi, chạy offline.

=== VAI TRÒ & TỰ DO ===
Bạn là GLM — Lead Engineer kiêm Game Director của studio. Được toàn quyền suy luận, định nghĩa và tinh chỉnh mọi hằng số (tune ±30%), bổ sung tiểu tiết kỹ thuật; KHÔNG được hạ bất kỳ tiêu chí nghiệm thu nào ở mục C.

=== A) AUTONOMOUS ITERATION LOOP (3 vòng tự trị) ===
Nguyên tắc vòng lặp: cuối MỖI vòng phải tự chạy checklist verify, in PASS/FAIL từng mục kèm số đo bằng chứng; nếu FAIL → sửa và lặp lại chính vòng đó (tối đa 3 lần lặp mỗi vòng) trước khi chuyển; chỉ kết thúc khi cả 3 vòng PASS toàn bộ.

[ITERATION 1 — CORE ENGINE]
- Canvas 1080x1920 portrait (9:16) HiDPI: scale theo devicePixelRatio (clamp 1.0–2.0), letterbox contain, tôn safe-area, resize không alloc; overlay FPS dạng dev (toggle).
- Fixed-step physics: SIM 120Hz, dt = 1/120s, accumulator + render interpolation alpha; dt thực clamp ≤ 50ms.
- Input 1-thumb duy nhất: pointerdown/move/up — giữ = neo tether tại điểm chạm, kéo = độ căng, thả = slingshot; touch-action: none, chặn scroll/zoom/double-tap-zoom.
- State machine: BOOT → MENU → PLAY → DEATH → REPLAY → RETRY; restart không reload trang; RNG mulberry32 seed theo run (deterministic).
- Verify vòng 1: cùng seed → cùng trajectory (dump 600 frame vị trí, diff = 0); 60 FPS ổn định; chơi trọn vẹn 1 run bằng 1 ngón trên mobile/emulator.

[ITERATION 2 — ENTERPRISE JUICE]
- Particle pool 640 pre-alloc + free-list swap-remove; KHÔNG new/splice trong game loop; tail comet = ring buffer 24 điểm vẽ ribbon gradient.
- Screen shake trauma model: shake = trauma², offset = 14px·shake·noise, trauma decay 1.6/s; cộng trauma 0.4 (graze) / 0.6 (sling) / 1.0 (death).
- Hitstop: timeScale = 0 chủ động 60ms (graze), 90ms (perfect sling), 150ms (death) — vẫn render, không block rAF.
- Procedural WebAudio 0-asset + unlockAudio(): resume AudioContext trong gesture đầu tiên; master chain gain 0.5 → DynamicsCompressor (threshold −24dB, ratio 4) → destination; music generative scheduler theo Web Audio clock (lookahead 120ms, không setInterval); voice spec ở B6.
- Verify vòng 2: heap snapshot sau 3 phút chơi delta < 512KB; 100% sự kiện game đều có SFX; không click/pop (attack ≥ 5ms, release có ramp).

[ITERATION 3 — ENTERPRISE HARDENING]
- Visibility API: visibilitychange → pause sim + audioCtx.suspend(); resume khi visible; chống dt-spike khi quay lại tab (không chết oan, không nhảy camera).
- LocalStorage key starlash_v1: {v:1, best, totalDust, runs, muted} — try/catch toàn bộ, ghi debounce 1s, nút mute bấm được.
- Near-miss hitbox: hitbox va chạm của comet = 80% bán kính hiển thị (−20%); graze ring +80px; graze tính khi |v| > 420 px/s, mỗi asteroid chỉ 1 lần.
- Instant Replay: ring buffer 120 mẫu @60Hz (player, asteroid alive, dust, camera); khi chết chuyển REPLAY ngay trong frame va chạm → phát 1.5s cuối ở slow-mo 0.35x + vignette; độ trễ va chạm → frame replay đầu < 150ms (đo timestamp); xong replay mới hiện GAME OVER.
- Verify vòng 3: kill tab/restore không lỗi; xoá storage vẫn chạy; replay không giật; đo latency replay < 150ms.

=== B) TECHNICAL BLUEPRINT (Director spec — cho phép tune ±30%) ===
B1. Canvas & Render: design space 1080x1920; setTransform(dpr·s); UI font stack monospace hệ thống; chuỗi UI chỉ rebuild khi giá trị đổi (dirty-flag); mọi thực thể pre-alloc (MAX_AST 24, MAX_DUST 32, POOL 640); scratch vector cấp phát module-level; không array/object literal, closure hay string-concat trong update/render.
B2. Vật lý (px, s; mass = 1): comet: a = tether + gravityWells + drag; v += a·dt; p += v·dt; |v| clamp 2600 px/s. Tether khi giữ: a_s = K·(anchor − p) − DAMP·v với K = 26 /s², DAMP = 2.2 /s; khoảng cách clamp 560px; tension T = dist/560. Khi thả: nếu T > 0.25 → cộng boost T²·620 px/s theo hướng normalize(p − anchor); PERFECT SLING khi T ≥ 0.85. Gravity well: a_g = GM·(s − p)/(r² + 200²)^1.5 với GM = 6e10 px³/s². Drag: v *= 1/(1 + 0.35·dt). Tường 2 bên bounce hệ số 0.55; rơi khỏi đáy camera = chết. Camera chỉ đi lên: cameraY = min(cameraY, playerY − 0.42·H), lerp 8/s; scroll floor 60 → 220 px/s theo thời gian.
B3. Spawn & difficulty: asteroid interval max(0.55, 1.6 − t·0.012)s; mỗi 900–1400px độ cao spawn 1 star kèm cụm 3–6 stardust quầng quanh; toàn bộ hằng số gom trong CONFIG object frozen đầu file.
B4. Palette (neon deep-space): nền gradient dọc #050112 → #160B2E (45%) → #2B1548; nebula radial #3F1D6E alpha 0.18 trôi chậm; comet core #FFF8E7, glow #FFD166; tail gradient #FFE066 → #FF006E → #7B2FF7 (alpha 0.9→0); tether line gradient #7DF9FF → #4CC9F0, alpha 0.35 + 0.5·T, width 2 + 6·T; stardust #FFE38A glow #FF9E00; asteroid #2E3244 rim #9AA3B8 crack #12141F; danger telegraph #FF477E; UI #E8E6F8; combo pop #FFD166.
B5. Combo & scoring: mỗi stardust +1 combo, timer decay 2.5s (hết giờ combo về 0); graze +2 combo và +150·m điểm; perfect sling +300·m; multiplier m = 1 + 0.5·floor(combo/5), cap 8.0; score = altitude·0.05 + 50·m mỗi dust + graze + sling; grade S/A/B/C theo ngưỡng score (tune); combo text pop-tween scale 1.4 → 1.0 trong 200ms.
B6. Audio synth (0 asset, voice table): pickup = sine theo pentatonic A minor [220.00, 261.63, 293.66, 329.63, 392.00, 440.00, 523.25, 659.25] Hz, index = combo mod 8, env attack 5ms / decay 180ms, gain 0.18, kèm triangle +1 octave gain 0.06; sling release = white-noise 0.12s qua bandpass 500 + 1300·T Hz; graze = sawtooth sweep 90 → 55Hz trong 90ms gain 0.22; death = sine sweep 130 → 28Hz 450ms + noise lowpass 900Hz 300ms; music bed = drone A2 110Hz sine + E3 164.81Hz triangle detune ±4 cent, hi-hat = noise 30ms highpass 6kHz mỗi nốt eighth, BPM = 128 + min(32, altitude/120).

=== C) ENTERPRISE ACCEPTANCE CRITERIA (nghiệm thu cuối — bắt buộc 100% PASS) ===
C1. Standalone 100%: đúng 1 file index.html; chạy trực tiếp bằng file:// offline; DevTools Network = 0 request sau khi load.
C2. 0 asset ngoài: không ảnh/font/lib/JSON ngoài; graphics 100% Canvas2D; audio 100% WebAudio procedural; font hệ thống.
C3. 60 FPS locked: trung bình ≥ 59.5 FPS, p95 frame time ≤ 17ms (hitstop/shake chủ động không tính là drop); không longtask > 50ms.
C4. 0 GC allocation trong RAF: không cấp phát object/array/string trong update + render; heap snapshot trước/sau 5 phút chơi delta < 512KB, không force GC.
C5. One-thumb 100%: toàn bộ gameplay + UI thao tác bằng 1 pointer; hit-target ≥ 44px; 5 giây đầu có ghost-hand tutorial không chặn chơi.
C6. Crash-free: soak test 10 phút không exception (hook window.onerror in ra dev overlay).

TRÌNH TỰ BẮT BUỘC: Iteration 1 → verify → Iteration 2 → verify → Iteration 3 → verify → chạy toàn bộ mục C → in báo cáo nghiệm thu cuối (bảng PASS/FAIL + số đo). 100% PASS mới được coi là hoàn thành.
```
