# FluxFlip: Neon Polarity Surge

- **Ngày:** 2026-08-22
- **Genre:** Arcade Physics Flow / One-Thumb Endless Climber — đảo cực từ tính để slingshot qua dàn anchor, graze hazard nuôi combo
- **Mô tả:** Bạn là một quả cầu từ tính rơi tự do trong ống conduit neon vô tận — chỉ với MỘT ngón tay, đảo cực (cyan↔magenta) để bị hút hoặc bị đẩy bởi các anchor, slingshot bay lên thay vì rơi xuống. Càng liều lĩnh lướt SÁT hazard (graze), flux và combo càng nổ → chuẩn hóa độ nghiện kiểu 'chỉ một run nữa'.
- **Nguồn:** GLM-5.3 (Z.AI Coding Plan)

---

## Master Prompt (Production-Ready)

```text
/goal — SHIP FLUXFLIP như một sản phẩm thương mại của Enterprise Arcade Studio (KHÔNG phải demo sinh viên): 1 file index.html duy nhất, cold-load sang playable < 2s, 60 FPS locked trên Android trung cấp 2020, vòng lặp core 30 giây gây nghiện, thương mại hoá được (meta-coin, 6 skin unlock, best-score chasing). Mọi quyết định kỹ thuật phục vụ 3 trụ cột: RESPONSIVE (input→pixel < 16ms), READABLE (mỗi cái chết đều do lỗi người chơi nhận diện được trong 1 lần xem replay), REPLAYABLE (spawner procedurally seeded — không 2 run nào giống nhau).

AUTONOMOUS ITERATION LOOP — tự lặp qua 3 giai đoạn dưới đây. Sau MỖI iteration: tự chạy checklist nghiệm thu của iteration đó; nếu fail → tối đa 2 vòng tự sửa rồi mới sang bước kế; nghiêm cấm regress các tiêu chí đã pass của iteration trước.

ITERATION 1 — CORE ENGINE (nền móng, chưa juice):
• Canvas 1080×1920 design-space, scale theo devicePixelRatio (cap 2.5), letterbox giữ aspect 9:16, context getContext('2d',{alpha:false}); resize handler với debounce.
• Fixed-step physics: SIM_DT = 1/120s, accumulator pattern, tối đa 5 substeps/frame (spiral-of-death guard), render interpolation alpha = acc/SIM_DT; tách tuyệt đối update(dt) và render(alpha) — không cộng dồn state trong render.
• 1-thumb input: pointerdown BẤT KỲ ĐÂU trên canvas = flip polarity (cyan↔magenta); touch-action:none + overscroll-behavior:none chặn iOS bounce; input queue đọc ở đầu frame để không mất event giữa 2 substep; flip cooldown 90ms chống spam.
• Vật lý (đơn vị design-space): gravity g = 1500 px/s²; lực từ mỗi anchor F = K·P/max(d², D_MIN²) với K = 9.2e6, D_MIN = 90px, P = ±1 theo cực (cùng cực → đẩy, khác cực → hút); drag tuyến tính 0.12/s; clamp vận tốc 2400 px/s; tường conduit restitution 0.55. Tích phân semi-implicit Euler: v += a·dt; x += v·dt.
• Spawner seeded RNG (mulberry32, seed = Date.now() xor run-id): anchor ribbon dạng sóng sin, hazard gate (cặp cột khe hẹp co dần), coin arc; tốc scroll = 260 + 8·floor(score/1000), cap 900 px/s; mật độ anchor đảm bảo luôn có đường sống còn (flood-check offline lúc dev).
• Game state machine: BOOT → MENU → RUN → DEATH → REPLAY → RUN; mọi chuyển state an toàn khi tab ẩn.

ITERATION 2 — ENTERPRISE JUICE (cảm giác AAA, 0 asset):
• ParticlePool 2048 hạt preallocate dưới dạng typed-array ring buffer (Float32Array cho x,y,vx,vy,life,maxLife,size,hueIdx) — CẤM new trong vòng lặp; spark 14 hạt/graze, bùng 40 hạt/death.
• Screen shake theo trauma²: offset = trauma²·26px·noise(t·40), pitch ±3.5°·trauma², decay 1.6/s; hitstop 180ms khi chết, micro-hitstop 40ms mỗi mốc chain 10.
• Procedural WebAudio 0-asset: unlockAudio() ở gesture đầu tiên (resume AudioContext + fade-in master 10ms — không tiếng 'pop'); master chain: DynamicsCompressor(threshold −18dB, ratio 6:1) → masterGain 0.8. Bảng âm: flip = square sweep 160→320Hz/60ms; graze = sine thang pentatonic minor [0,3,5,7,10] semitone base 440Hz, leo 1 bậc/chain + octave mỗi 5 chain (cap +2), osc thứ hai detune ×2.01 tạo shimmer; slingshot = triangle 220→440Hz/90ms; death = saw 300→40Hz/500ms + noise burst lowpass 800Hz; OVERDRIVE pad = 2 saw 110Hz detune ±7 cents, LFO 6Hz quét filter cutoff 400→2400Hz. Peak mọi âm ≤ −6dB.
• Trail ribbon 24 điểm gần nhất cho player orb, globalCompositeOperation = 'lighter'; hit-flash radial trắng 60ms alpha 0.5→0; graze ring sáng #FFE066 quanh hazard khi vào annulus.
• Combo system: graze window 1.4s (refresh mỗi graze), multiplier = 1 + floor(chain/3), cap ×16; Flux Meter 0→100% (+4/graze, +12/coin); đầy 100% → OVERDRIVE 6s: time-scale 0.85, score ×2, toàn scene dịch hue +15°, vignette pulse theo nhịp LFO.

ITERATION 3 — ENTERPRISE HARDENING (thành sản phẩm thật):
• Visibility API: visibilitychange → pause loop + audioContext.suspend(); quay lại tab → đếm ngược 3-2-1 (600ms) rồi resume — physics KHÔNG lấy dt lớn (reset lastTime).
• LocalStorage versioned + try/catch quota/schema: keys fluxflip.best, fluxflip.coins, fluxflip.settings; schema {v:1, best:number, coins:number, skins:string[], settings:{sfx:bool,mus:bool,quality:0|1|2}}; lưu best ngay lúc chết, không chờ unload.
• Near-miss hitbox −20%: hitbox CHẾT thu nhỏ còn 0.8·(r_hazard + r_player) (lethal khi d < ngưỡng này); graze annulus 0.8→1.35× tổng bán kính → thưởng flux + chain + spark — người chơi được ĐỀN BÙ cho việc chơi liều.
• Instant Replay < 150ms: ring buffer 150 mẫu @30Hz (player pos/vel/polarity + subset hazard positions, Int16 quantized); khi chết playback 0.5× tốc độ với滤镜 sepia + letterbox; khung đầu tiên hiển thị < 150ms (chỉ reset con trỏ đọc buffer — CẤM allocate); tap để skip replay → retry.
• FPS governor: trung bình trượt 60 frame < 52fps kéo dài 3s → tự hạ tier quality: budget particle −50% → tắt trail → tắt trail + shake; hiện icon quality góc màn hình.
• Safe-area inset cho notch; chặn double-tap-zoom;.Screen Wake Lock API (best-effort, try/catch).

ENTERPRISE ACCEPTANCE CRITERIA (nghiệm thu cuối — fail 1 mục = KHÔNG ship):
1. Standalone 100%: duy nhất 1 file index.html chạy trực tiếp qua file://, 0 network request (DevTools Network panel = 0), 0 asset ngoài, 0 thư viện ngoài, 0 font ngoài (ui-monospace stack).
2. 60 FPS locked: 1% low ≥ 55fps trên thiết bị trung cấp; không có frame spike > 18ms lặp lại trong 10 phút chơi.
3. 0 GC allocation trong RAF: Allocation instrumentation trên timeline PHẲNG sau 3 phút; update()/render() không chứa object literal, closure, map/filter/concat, array push ngoài ring buffer — mọi vector/biến tạm preallocate ở module scope và tái sử dụng.
4. Cold load → playable < 2s; chết → retry < 1.5s (kể cả skip replay); menu → run < 300ms.
5. 0 lỗi, 0 cảnh báo console; 'use strict' toàn cục; IIFE duy nhất, module hoá bằng const namespace (Input, Sim, FX, Audio, Store, Replay, Loop) — không ES module để tương thích file://.
6. Stress test: 10 phút xen kẽ fore/background tab + lock/mở màn hình → không lệch physics (fixed-step đảm bảo), không méo audio, không mất save.
7. Readable death: 10 ca chết thử nghiệm — mỗi ca người chơi xác định được nguyên nhân trong 1 lần xem replay; graze reward RÕ RẠNG về mặt thị giác + thính giác.

TECHNICAL BLUEPRINT:
• Bảng màu: nền gradient dọc #05060F → #0B1030 → #1A0B2E + 3 lớp sao tĩnh parallax (tốc 0.2/0.5/0.9×); cực cyan #00F0FF (glow rgba(0,240,255,0.35)) ↔ magenta #FF2E88; hazard trắng nóng #FFFFFF lõi amber #FFB300; graze ring #FFE066; coin = hexagon nhỏ #7CFFB2; overdrive toàn scene filter hue +15°.
• Công thức tổng hợp mỗi substep: a = g·ĵ + Σ(K·P_i/max(d_i², D_MIN²))·û_i − 0.12·v; chú ý anchor chỉ tác dụng trong bán kính ảnh hưởng 560px để giới hạn vòng lặp.
• Audio table đúng như Iteration 2; mọi node ngắn hạn tạo trong event handler (ngoài RAF), unlock một lần duy nhất.
• Meta loop: coin (+12/coin thu, ×2 trong overdrive) → unlock 6 skin procedural palette đổi cặp màu cực (cyan/magenta ↔ lime/violet ↔ gold/crimson...) — chỉ đổi biến màu, 0 asset.
• Quy ước code: hằng số IN_HOA module scope; mọi hàm hot viết flat, no-GC; comment tiếng Anh, UI text tiếng Việt.
```
