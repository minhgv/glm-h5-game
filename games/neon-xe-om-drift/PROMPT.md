# Prompt & PRD: Neon Xe Om: Sài Gòn 2087

**Genre:** Arcade / Endless Traffic-Weaver (one-thumb, risk-reward combo)
**Generated by:** GLM-5.3

## Master Prompt:
```text
Xây dựng game H5 'Neon Xe Om: Sài Gòn 2087' — 1 file index.html duy nhất, không phụ thuộc thư viện ngoài, chạy offline trên desktop + mobile.

=== 1. KỸ THUẬT NỀN ===
- Canvas 2D fullscreen (devicePixelRatio-aware), responsive giữ aspect 9:16 trên mobile, letterbox trên desktop.
- Game loop: requestAnimationFrame + delta-time clamp (dt max 50ms), fixed-timestep physics 120Hz với accumulator, render interpolation không bắt buộc.
- Object pooling cho mọi entity (traffic, particle, floating text). Không GC spike: không tạo object/array trong loop.
- Font: hệ thống (system-ui, 'Segoe UI', sans-serif); không load font ngoài.

=== 2. VISUAL / ART DIRECTION ===
- Cyberpunk Sài Gòn: trời tím-đen gradient (#0a0618 -> #1a0b2e), 3 lane-highway dọc với perspective giả (lanes thu hẹp về chân trời y=0.15H).
- Palette neon: cyan #00f0ff, magenta #ff2fb9, vàng hổ phách #ffb800 (đèn đường), xanh lá #39ff88.
- Parallax 4 lớp: skyline tháp neon + Bến Thành mái bay Nguyen-style silhouette, bảng hiệu 'PHỞ 24/7', 'CÀ PHÊ MŨ', billboard glitch, grid đường sàn neon chạy về phía người chơi (tạo cảm giác tốc độ).
- Player: xe máy top-down 3/4 vẽ bằng canvas path (thân + ánh sáng underglow + vệt light-trail khi turbo). Lean angle khi chuyển lane, shadow blur glow.
- Hiệu ứng: speed lines khi turbo, screen shake khi crash, chromatic-aberration giả (vẽ overlay đỏ/cyan lệch 2px) khi combo >= 10, rain neon (particle chéo) mỗi 30s.

=== 3. GAMEPLAY LOOP ===
- States: MENU -> COUNTDOWN(3,2,1) -> PLAYING -> GAMEOVER (hiện score, best, nút Chơi lại). ESC/P pause.
- Điều khiển: giữ nửa trái màn hình = lái trái, nửa phải = lái phải (mượt, không phải snap-lane). Desktop: ArrowLeft/Right hoặc A/D. Space/Double-tap = kích hoạt Turbo khi đầy meter.
- Loop nghiện: luồn lách -> near-miss (+điểm, +combo, sạc turbo) -> kích hoạt turbo (điểm x2, xuyên 'photon mode' hủy 1 xe) -> tốc độ tăng dần -> rủi ro cao hơn -> best score -> retry trong < 2 giây.

=== 4. PHYSICS (chính xác, đơn vị: canvas-unit/giây) ===
- Player: x-position liên tục (không phải discrete lane). Lateral: accel 3200 u/s² khi giữ, drag exponential damping ~ e^(-6dt), max lateral speed 620 u/s. Lean angle = -lateralVel/620 * 0.45 rad, lerp mượt.
- Tiến: worldSpeed bắt đầu 300 u/s, tăng +8 u/s mỗi 10s, cap 900. Turbo nhân 1.7x trong 3s.
- Traffic: spawn theo 'sóng' pattern (sin lane + random), mỗi xe có speed riêng 0.45–0.75x worldSpeed, đi lên màn hình (tương đối). Mật độ tăng theo difficulty = 1 + elapsed/45 (cap 2.2).
- Va chạm: circle-vs-rounded-rect (player r=16, traffic hitbox thu nhỏ 12% cho 'fair feeling').
- Near-miss: khi khoảng cách mép-mép < 26u và không va chạm trong lúc xe truy cập vượt qua -> trigger near-miss.
- Obstacles đa dạng: xe máy chậm, 'container bay' (chiếm 1.5 lane, phải né xa), rào chắn neon (1 lane, di chuyển ngang chậm), cụm 3 xe hình thoi (chỉ 1 khe thoát).

=== 5. SCORING & COMBO ===
- Điểm nền: distance/10 mỗi frame-rate-independent tick.
- Near-miss: base 50 x comboMultiplier. Combo tăng +1 mỗi near-miss, reset nếu 4s không có near-miss hoặc bị va chạm. Multiplier = 1 + floor(combo/5), cap x8.
- Turbo kill (photon xuyên xe): +200.
- Floating text '+150 NEAR-MISS x4' bay lên fade-out tại vị trí xảy ra.
- High score + tổng near-miss lưu localStorage key 'neonxeom_best'.'
- Medal khi game over: Đồng <2k, Bạc <6k, Vàng <15k, Kim Cương >=15k.

=== 6. AUDIO — WEBAUDIO PROCEDURAL 100% (không file âm thanh) ===
- AudioContext khởi tạo lazy sau gesture đầu tiên. Master chain: masterGain -> compressor -> destination.
- ENGINE: 2 oscillator (sawtooth 55Hz + square 110Hz detune 7 cents) -> lowpass filter cutoff map theo speed (400–2400Hz) -> gain 0.06. Turbo: thêm sub sine 27Hz + nâng cutoff, playbackRate cảm giác qua LFO 8Hz nhẹ trên gain.
- NEAR-MISS WHOOSH: buffer noise 0.25s qua bandpass sweep 300->3800Hz trong 0.18s, gain envelope nhanh — pitch tăng nhẹ theo combo (bandpass x (1 + combo*0.02)).
- CRASH: noise burst 0.6s + distortion WaveShaper + drop engine pitch về 0 trong 0.4s.
- TURBO: rising sine sweep 200->900Hz + shimmer (square 5ths) 0.3s.
- SCORE TICK combo-up: pentatonic blur (A minor pentatonic: A4 C5 D5 E5 G5) pick note theo combo index — tạo cảm giác 'lên dậy'.
- MUSIC: procedural synthwave loop — scheduler lookahead 25ms/0.1s: bass line sawtooth pattern 8-note (Am F C G), kick (sine drop 150->40Hz) mỗi beat, hi-hat (highpass noise 0.03s) off-beat, pad (2 detuned saw qua lowpass 800Hz, attack 0.5s) đổi chord mỗi 4 bar. BPM 118, tăng +6 BPM mỗi lần difficulty tăng (cap 150).
- Mute toggle (icon loa, phím M), lưu preference localStorage.

=== 7. UI/HUD ===
- HUD Canvas-ve: score trái trên (font 700, glow cyan), combo meter (thanh decay thời gian còn lại của combo), turbo meter dưới cùng (gradient magenta->vàng, rung khi đầy), speed km/h phải trên (worldSpeed/3 hiển thị).
- MENU: logo chữ 'NEON XE OM' glow flicker, tagline 'Sài Gòn 2087 — Luồn không chạm', nút Chơi (touch-friendly >= 48px), Best score, hướng dẫn 2 dòng ngắn.
- GAMEOVER: panel tối mờ, thống kê (Score, Near-miss count, Distance, Best), nút 'Chạy lại' + 'Menu'.
- Toàn bộ text UI song ngữ Việt; vui tươi nhưng gọn.

=== 8. PERFORMANCE & CHẤT LƯỢNG ===
- Target 60fps trên điện thoại trung cấp: <= 40 traffic entities, <= 150 particles, tắt shadowBlur trên path phức tạp khi fps < 50 (auto-degrade), limit DPR <= 2.
- Pause tự động khi tab hidden (visibilitychange).
- Không dùng alert/confirm; mọi thông báo bằng in-game overlay.
- Code tổ chức rõ trong <script>: const CONFIG object đầu file chứa toàn bộ hằng số tuning (GRAVITATIONAL-free physics constants, spawn tables, audio freq maps) để chỉnh dễ.

=== 9. ACCEPTANCE CRITERIA ===
- Mở file = chơi được ngay, không lỗi console.
- Near-miss cho cảm giác 'sướng tay' rõ rệt (whoosh + flash + điểm pop).
- Chơi 3 lần liên tiếp muốn phá kỷ lục (risk-reward cân bằng: chết do tham lam, không do rng xấu).
- Âm thanh tắt/mở hoạt động; music không bị rò rỉ node (stop + disconnect đúng cách).
- Full chơi được bằng 1 ngón tay cái trên mobile.
```
